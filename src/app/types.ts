export interface Drinks {
    rank: number
    brand: string
    crp: number
}

export interface Fastfood {
    brand: string
    storeQuantity: number
    startingDate: Date
    upToDate: Date
    infoSource: string
}