export interface Drinks {
    brand: string
    crp: number
    rank: number
}

export interface Fastfood {
    brand: string
    storeQuantity: number
    startingDate: Date
    upToDate: Date
    infoSource: string
}